{
  "name": "blep-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
  "test": "IN_MEMORY_DB=1 vitest run --exclude \"e2e/**\"",
    "test:watch": "vitest",
    "migrate": "node-pg-migrate -m migrations -d $PGDATABASE",
    "migrate:up": "node-pg-migrate up -m migrations -d $PGDATABASE",
    "migrate:down": "node-pg-migrate down -m migrations -d $PGDATABASE",
    "migrate:create": "node-pg-migrate create -m migrations",
    "seed": "node scripts/seed.js",
    "test:e2e": "vitest run e2e"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "node-pg-migrate": "^7.6.2",
  "nodemon": "^3.1.4",
  "supertest": "^6.3.4",
  "vitest": "^1.6.0"
  }
}
